{
  "id": "searnxg-search",
  "name": "SearXNG Search",
  "description": "Query a self-hosted SearXNG instance from OpenClaw.",
  "version": "0.0.1",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "baseUrl": {
        "type": "string",
        "default": "http://localhost:8080",
        "description": "Base URL for your local SearXNG instance."
      }
    }
  },
  "uiHints": {
    "baseUrl": {
      "label": "SearXNG Base URL",
      "placeholder": "http://localhost:8080"
    }
  }
}
